apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: nginx
  name: nginx2
spec:
  replicas: 3
  selector:
    matchLabels:
      run: nginx2
  template:
    metadata:
      labels:
        run: nginx2
    spec:
      containers:
      - name: nginx
        image: nginx:1.16
